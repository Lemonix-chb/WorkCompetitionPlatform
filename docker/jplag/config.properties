# JPlag Configuration
# 代码重复检测服务配置

sensitivity=DEFAULT
min-match-length=10
result-dir=/data/results
temp-dir=/data/temp

# 比较算法设置
comparison-mode=DEFAULT

# 语言支持
language=JAVA

# 结果格式
output-format=JSON

# 性能设置
parallel-comparison=true
max-threads=4